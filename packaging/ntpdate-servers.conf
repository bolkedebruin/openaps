# Operator-editable NTP server list. Space-separated hostnames; sourced by
# /etc/rc5.d/S99-ntpdate and passed to busybox `ntpd` as repeated -p peers.
NTPSERVERS="0.debian.pool.ntp.org 1.debian.pool.ntp.org 2.debian.pool.ntp.org 3.debian.pool.ntp.org"
