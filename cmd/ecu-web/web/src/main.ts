// Entry point: pulls in the app shell (which transitively registers all
// views and components) and mounts it. Bundled by build.ts into
// dist/assets/main.js and embedded into the ecu-web binary.
import "./app-shell.ts";
